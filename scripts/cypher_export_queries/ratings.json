{
  "statements":[
    {"statement":
     "MATCH (n:`Rating`)-[:IS_RATED_WITH]-(e:Entry)
      RETURN
       n.id as id,
       n.created as created,
       n.title as title,
       n.context as context,
       n.value as value,
       e.id as entry_id"
    }
  ]
}
