{
  "statements":[
    {"statement":
     "MATCH (r:`Rating`)-[:IS_RATED_WITH]-(e:Entry)
      RETURN
       r.id as id,
       r.created as created,
       r.title as title,
       r.value as value,
       r.context as context,
       r.source as source,
       e.id as entry_id"
    }
  ]
}
