{
  "statements":[
    {"statement":
     "MATCH (n:`Comment`)-[:IS_COMMENTED_WITH]-(r:`Rating`)
      RETURN
       n.id as id,
       n.created as created,
       n.text as text,
       r.id as rating_id"
    }
  ]
}
