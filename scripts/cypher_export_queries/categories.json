{
  "statements":[
    {"statement":
     "MATCH (n:Category)--(s:CategoryState)
      RETURN
       n.id as id,
       s.created as created,
       s.version as version,
       s.name as name"
    }
  ]
}
