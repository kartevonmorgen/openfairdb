{
  "statements":[
    {"statement":
     "MATCH (n:`User`)
      RETURN
       n.id       as id,
       n.username as username,
       n.password as password,
       n.email as email,
       n.email_confirmed as email_confirmed"
    }
  ]
}
